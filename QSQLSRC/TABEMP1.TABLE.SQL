CREATE TABLE TABEMP1(
    EMP_ID     INT GENERATED ALWAYS AS IDENTITY,
    EMP_NAME   VARCHAR(50),
    DEPT       VARCHAR(20),
    SALARY     DECIMAL(10,2),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
ALTER TABLE TABEMP1
ADD CONSTRAINT EMPCONS UNIQUE (EMP_ID);
